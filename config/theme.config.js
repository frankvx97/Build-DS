/**
 * BUILD DESIGN SYSTEM - THEME CONFIGURATION (DEPRECATED FOR NOW)
 * 
 * This file controls which token modes are active in your design system.
 * Simply change the values below to switch between different themes and modes.
 * 
 * Available options:
 * - colorMode: 'Light' or 'Dark'
 * - neutralMode: 'Gray' or 'Slate'
 * - themeMode: 'Blue', 'Purple', 'Orange', or 'Sky'
 */

export const themeConfig = {
  // Color mode: Controls overall appearance
  colorMode: 'Light',  // Options: 'Light' | 'Dark'
  
  // Neutral palette: Defines the grey/neutral colors used throughout
  neutralMode: 'Gray',  // Options: 'Gray' | 'Slate'
  
  // Theme: Controls the primary brand color
  themeMode: 'Blue'  // Options: 'Blue' | 'Purple' | 'Orange' | 'Sky'
};

export default themeConfig;
